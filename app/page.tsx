import { products } from "../data/products";
export default function Home() {
  return <main><header><a className="brand" href="#top" aria-label="Wikz product gallery home"><b>W/</b> Products</a><span className="availability"><i/>Building in public</span></header>
    <section className="hero" id="top"><p className="kicker">Selected work · 2026</p><h1>Useful ideas,<br/><em>made tangible.</em></h1><p className="intro">A small collection of tools and experiments built to make complex workflows feel clear, calm, and practical.</p><a href="#products" className="explore">Explore the collection <span aria-hidden="true">↓</span></a></section>
    <section className="collection" id="products" aria-labelledby="collection-title"><div className="heading"><h2 id="collection-title">The collection</h2><span>02 projects</span></div><div className="grid">{products.map((product)=><article className={`card ${product.accent}`} key={product.number}><div className="cardTop"><span>{product.number}</span><span className="status"><i/>{product.status}</span></div><div className="art" aria-hidden="true"><div className="orb"/><strong>{product.number === "01" ? "O×H" : "Aa"}</strong><div className="lines"/></div><div className="cardBody"><p className="category">{product.category}</p><h3>{product.title}</h3><p className="description">{product.description}</p><ul aria-label={`${product.title} features`}>{product.features.map((feature)=><li key={feature}>{feature}</li>)}</ul><a className="openApp" href={product.url} target="_blank" rel="noreferrer"><span>Open App</span><span aria-hidden="true">↗</span></a></div></article>)}</div><p className="note">Two verified products, available to explore now.</p></section>
    <footer><span>Independent web experiments by Wikz.</span><a href="#top">Back to top ↑</a></footer>
  </main>;
}
